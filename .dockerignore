# Contexto de build enxuto — o Dockerfile faz `COPY . .`
node_modules
.next
out
.git
.gitignore

# segredos — NUNCA vão para a imagem
.env
.env.local
.env.*.local
.env.production

# supabase local (não é usado no deploy)
supabase/.branches
supabase/.temp

# dev / local
docker-compose.yml
tsconfig.tsbuildinfo
*.log
.DS_Store
.vscode
docs
README.md
